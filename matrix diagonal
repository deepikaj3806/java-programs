class Solution {
    public int diagonalSum(int[][] arr) {
        int sum=0;
        int n=arr.length;
        for(int i=0;i<n;i++){
            sum += arr[i][i];
            sum += arr[i][n-i-1];
        }
        return (n&1)==1? sum-arr[n/2][n/2]:sum;
    }
}
